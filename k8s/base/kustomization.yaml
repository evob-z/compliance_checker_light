# =============================================================================
# Kustomize 基础配置
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - secret-template.yaml

commonLabels:
  app: compliance-checker
  managed-by: kustomize

images:
  - name: ghcr.io/evob-z/compliance_checker_light
    newTag: latest
