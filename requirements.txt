# -------------------- MCP 框架 --------------------
mcp>=1.0.0

# -------------------- LLM 客户端 --------------------
# OpenAI 兼容 API 客户端（支持 DashScope、Moonshot、DeepSeek 等）
openai>=1.0.0

# -------------------- 文档解析 --------------------
# PDF 解析（PyMuPDF）
PyMuPDF>=1.23.0

# Word 文档解析
python-docx>=0.8.11

# -------------------- 图像处理 --------------------
Pillow>=10.0.0

# -------------------- 异步 HTTP 客户端 --------------------
aiohttp>=3.9.0

# -------------------- 配置与数据 --------------------
# YAML 解析
PyYAML>=6.0

# 环境变量管理
python-dotenv>=0.21.0

# =============================================================================
# 可选依赖（按需安装）
# =============================================================================

# -------------------- OCR 相关（可选） --------------------
# 注意：OCR 默认不启用，仅在处理扫描件时需要
# 可通过环境变量 OCR_BACKEND 配置：none（默认）/ paddle / aliyun

# [本地 OCR] PaddleOCR - 体积大(~500MB)但无需网络
# paddleocr>=2.7.0
# paddlepaddle>=2.5.0

# [云端 OCR] 阿里云 OCR - 轻量但需网络和付费
# alibabacloud_ocr_api20210707>=1.0.0
# alibabacloud-tea-openapi>=0.3.0

